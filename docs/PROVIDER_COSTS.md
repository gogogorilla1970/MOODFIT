# Provider cost note

The initial v0.2 provider is fal.ai FLUX.2 edit. Provider pricing can change; check the provider before large batch use. The architecture keeps provider selection replaceable so a self-hosted backend can be added later.
